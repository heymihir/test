columns_to_update = ['Name', 'Amount']
mapping = {'Name': 'New Name', 'Amount': 'New Amount'}

mask = df_updated['CUST_ID.1'].isin(df_new_customers['Cust Id'])
for col in columns_to_update:
    df_updated.loc[mask, col] = df_updated.loc[mask, 'CUST_ID.1'].map(
        df_new_customers.set_index('Cust Id')[mapping[col]]
    )

print(df_updated)
