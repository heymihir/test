import pandas as pd

# Filter columns containing '_eq_' in their names
eq_cols = merged.filter(like='_eq_')

# Create a summary table
summary = pd.DataFrame({
    'Column Name': eq_cols.columns,
    'Match Rate': eq_cols.mean(),             # Fraction of True values
    'Count True': eq_cols.sum(),              # Total count of True values
    'Count False': (~eq_cols).sum()           # Total count of False values
}).reset_index(drop=True)

print(summary)
