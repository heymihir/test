# Write both outputs to an Excel file with two sheets
with pd.ExcelWriter("output.xlsx") as writer:
    agg_balance_gb.to_excel(writer, sheet_name="Sheet1", index=False)
    agg_balance_cmb.to_excel(writer, sheet_name="Sheet2", index=False)