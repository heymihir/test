
# Reverse mapping from grouped_df to create product_desc in merged
desc_mapping = {product: desc for desc, products in grouped_df["Product 1"].items() for product in products}
merged["product_desc"] = merged["product_code"].map(desc_mapping)