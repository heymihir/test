# Create the mapping dictionary from Table 1
mapping = table1.set_index("Product 1")["prod desc"].to_dict()

# Map the values from table2 using the dictionary
table2["product_desc"] = table2["PRODUCT_CODE_dhruv_data"].map(mapping)
