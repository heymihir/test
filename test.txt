import pandas as pd

# 1. Unique count of customers where flag is True
unique_cust_count = df[df["flag"] == True]["cust_id"].nunique()
print("Unique cust_ids with flag=True:", unique_cust_count)

# 2. Sum of balance QoQ (grouped by year and quarter)
balance_qoq = df.groupby(["year", "quarter"])["balance"].sum().reset_index()
print("Sum of balance QoQ:")
print(balance_qoq)
